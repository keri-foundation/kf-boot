version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"
  apt_packages:
    - libsodium-dev
  jobs:
    post_install:
      - python -m pip install --no-deps -e .

sphinx:
  builder: dirhtml
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
